import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fatoora-reader-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
